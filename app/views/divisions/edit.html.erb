<% if @division.errors.any? %>
  <% @division.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h1>Edit Division #<%= @division.id %></h1>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_division/<%= @division.id %>" class="form-horizontal">
      <!-- Label and input for division -->
      <div class="form-group">
        <label for="division" class="col-sm-2 control-label">
          Division
        </label>
        <div class="col-sm-10">
          <input type="text" id="division" name="division" placeholder="Division" class="form-control" value="<%= @division.division %>">
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <input type="submit" value="Update Division" class="btn btn-primary">
        </div>
      </div>
    </form>
  </div>
</div>

